# Downloaded/compiled tooling - large binaries, regenerated by SKILL.md's Prerequisites steps
# rather than committed.
tools/msedgedriver.exe
tools/Driver_Notes/
tools/cargo-tauri-driver/
tools/session.json
tools/config.json.real-backup
tools/*.png

# Synthetic test video and everything the app itself writes alongside it (.edits.json sidecar,
# .trash/, Docs/) - regenerated by SKILL.md's fixture step, not committed.
fixtures/library/
